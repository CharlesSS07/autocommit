

move autocommit-env and autocommit-hook to a string in autocommit-setup, and have it write them into a script in .git/hooks/, which is called by .git/hooks/prepare-commit-msg

then make autocommit-setup take arguments like enable or disable, etc..
